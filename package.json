{
  "name": "smartdisplay-ai",
  "version": "1.0.0",
  "description": "SmartDisplay AI - Turn Android into a smart secondary display and control center",
  "private": true,
  "main": "electron/main.js",
  "scripts": {
    "start": "electron .",
    "dev": "node scripts/clean_api.js && next build && node scripts/fix_paths.js && electron .",
    "build:apk": "node scripts/build_only.js",
    "build": "node scripts/clean_api.js && next build && node scripts/fix_paths.js",
    "auto-sync": "node scripts/legacy/scratch_auto_sync.js",
    "test-conn": "node scripts/legacy/test_connection.js",
    "install:moonlight": "node scripts/legacy/scratch_install_moonlight.js",
    "start-sunshine": "node scripts/scratch_start_sunshine.js"
  },
  "dependencies": {
    "electron-updater": "^6.8.9",
    "ws": "^8.21.0"
  },
  "devDependencies": {
    "electron": "^42.4.0",
    "electron-builder": "^26.15.3"
  }
}
