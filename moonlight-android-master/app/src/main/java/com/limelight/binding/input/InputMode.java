package com.limelight.binding.input;

public enum InputMode {
    TOUCHPAD,
    MOUSE,
    NAVIGATION,
    GAMEPAD,
    HAND,      // Free viewport navigation (single-finger pan + pinch zoom)
    KEYBOARD   // On-screen virtual keyboard overlay
}
